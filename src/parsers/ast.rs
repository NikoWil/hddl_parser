pub struct BaseType {
    pub name: String,
}

pub struct Predicate {
    pub name: String,
}

pub struct Variable {
    pub name: String,
}

pub struct PrimitiveType {
    pub name: String,
}

pub struct TaskSymbol {
    pub name: String,
}

pub struct SubtaskId {
    pub name: String,
}

pub struct Term {
    pub name: String,
}
